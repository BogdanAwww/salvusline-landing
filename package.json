{
  "name": "salvusline-platform",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter site dev & pnpm --filter admin dev",
    "dev:site": "pnpm --filter site dev",
    "dev:admin": "pnpm --filter admin dev",
    "build:site": "NEXT_OUTPUT=export pnpm --filter site build",
    "build:admin": "pnpm --filter admin build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
